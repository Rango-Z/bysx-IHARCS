server:
  port: 8104

spring:
  application:
    name: datav
  data:
    mongodb:
      uri: mongodb://localhost:27017/smart_home

deps:
  userDeviceBaseUrl: http://localhost:8101

management:
  endpoints:
    web:
      exposure:
        include: health,info

